# time before autoboot
timeout: 3

/KaworuOS
    protocol: limine
    # path to the kernel to boot. boot():/ represents the partition on which limine.conf is located.
    path: boot():/boot/kaworu.elf
    # command line arguments
    cmdline: loglevel=1
