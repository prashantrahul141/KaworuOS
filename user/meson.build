foreach file : [
  'user_init.c'
]
    user_c_sources += files(project_root / 'user' / file)
endforeach

foreach file : [
  'user_init.S'
]
    user_asm_sources += files(project_root / 'user' / file)
endforeach
